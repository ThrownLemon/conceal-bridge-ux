## 2025-12-19 - Testing with Vitest
**Learning:** `ng test` handles `TestBed` environment initialization for Vitest. Running `npx vitest` directly requires manual setup or config.
**Action:** Always use `ng test` (or `npm test`) to run tests in this repo.
