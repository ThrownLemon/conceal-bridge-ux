---
description: A workflow to updating the project npm packages
---

use the console to run the 'ncu' command which is the node-check-updates tool.
if updates are found install them 'ncu -u'
install the packages 'npm install'