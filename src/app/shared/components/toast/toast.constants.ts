/**
 * Constants for toast notifications.
 */

/**
 * Animation duration for toast transitions (entrance/exit animations).
 * This value must match the Tailwind `duration-300` class used in toast.component.ts.
 */
export const TOAST_ANIMATION_DURATION = 300;
